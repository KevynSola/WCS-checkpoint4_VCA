<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>
			{% block title %}
				VCA
			{% endblock %}
		</title>
		<link rel="apple-touch-icon" sizes="180x180" href="{{asset('build/images/icons/apple-touch-icon.png')}}">
		<link rel="icon" type="image/png" sizes="32x32" href="{{asset('build/images/icons/favicon-32x32.png')}}">
		<link rel="icon" type="image/png" sizes="16x16" href="{{asset('build/images/icons/favicon-16x16.png')}}">
		<link
		rel="icon" type="image/png" href="{{asset('build/images/icons/favicon.ico')}}">
		{# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
		{% endblock %}

		{% block javascripts %}
			{{ encore_entry_script_tags('app') }}
		{% endblock %}
	</head>
	<header>
		{% if is_granted('ROLE_VISITOR') %}
			{% include "/components/_navbar.html.twig" %}
		{% endif %}
		{% if is_granted('ROLE_KILLER') %}
			{% include "/components/_navbarKiller.html.twig" %}
		{% endif %}
	</header>
	<body>
		{% block body %}{% endblock %}
	</body>
	<footer>
		<a href="{{path('app_legal')}}" class="fixed-bottom text-decoration-none text-light d-flex justify-content-end">Legal Notices</a>
	</footer>
</html>
