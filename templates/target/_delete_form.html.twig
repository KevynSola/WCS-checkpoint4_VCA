<form method="post" action="{{ path('app_target_delete', {'target': target.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ target.id) }}">
    <button class="btn btn-lg btn-blood">Delete</button>
</form>
