{% extends 'base.html.twig' %}

{% block title %}
    {{parent()}}| Dashboard
{% endblock %}

{% block body %}
    <div class="container">
        {% for killer in killers %}
            
        <h1>Dashboard {{killer.username}}</h1>
        <div class="card">
            
        </div>
        {% endfor %}
    </div>
{% endblock %}
